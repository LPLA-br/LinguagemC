# what flags you want to pass to the C compiler & linker
CFLAGS = --pedantic -Wall -std=c99 -O2
# LDFLAGS =
# this lists the binaries to produce, the () targets in
# the previous manual makefile
bin_PROGRAMS = teste
teste_SOURCES = teste.c
